version: 2
models:
  - name: dim_customer
    description: "Customer data."
    columns:
      - name: customer_key
        description: "Unique surrogate key for each customer"
        tests:
          - unique
      - name: customer_name
        tests:
          - not_null
      - name: customer_email
        tests:
          - not_null
